spring:
  application:
    name: trade-service  # 微服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.31.128:8848 # 虚拟机地址:nacos端口
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-jdbc.yaml
          - data-id: shared-log.yaml
          - data-id: shared-swagger.yaml
#          - data-id: shared-seata.yaml
          - data-id: shared-mq.yaml
          - data-id: shared-prometheus.yaml
#management:
#  endpoints:
#    web:
#      exposure:
#        include: '*' # 暴露 /actuator/prometheus 端点
#  endpoint:
#    prometheus:
#      enabled: true
#  metrics:
#    #  👇 关键：关闭 jdbc 级别的指标收集，防止 ShardingSphere 导致项目启动崩溃
#    enable:
#      jdbc: false
#    tags:
#      application: ${spring.application.name} # 为所有指标打上当前微服务名称的标签，方便多服务过滤