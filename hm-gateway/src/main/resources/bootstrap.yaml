spring:
  application:
    name: gateway  # 微服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.31.128:8848 # 虚拟机地址:nacos端口
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml