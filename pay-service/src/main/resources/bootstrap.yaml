spring:
  application:
    name: pay-service  # 微服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.31.128:8848 # 虚拟机地址:nacos端口
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-jdbc.yaml
          - data-id: shared-log.yaml
          - data-id: shared-swagger.yaml
         # - data-id: shared-seata.yaml
          - data-id: shared-mq.yaml
#      discovery: # 服务发现配置，用于nacos环境隔离，但是隔离后的服务之间无法调用无法发现
#        namespace: 782bbf1c-e82f-4e60-bee2-8cc08935045d # dev环境下的id